<div class="container">
  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">      
<!--       <h2>Vertical (basic) form</h2> -->
        <%= form_for @image do |form| %>
    <div class="form-group">
      <label for="email">Image:</label>
      <!-- <input type="text" class="form-control" id="image_image_name" placeholder="Enter image" name="image[image_name]"> -->
      <%= form.file_field :image_name, class: "form-control"%>
    </div>
    <div class="form-group">
      <label for="pwd">Country:</label>
      <%#= form.collection_select :country_id, Country.order(:country_name),:id,:country_name, include_blank: true, {:prompt => 'Select Country'} %>
      <%#= select("post", "person_id", Person.all.collect {|p| [ p.name, p.id ] }, {:prompt => 'Select Person'})%>
       
      <%= form.select(:company_id, Company.all.map{|c| [c.company_name, c.id]}, {include_blank: "Select Company"}, { :class => 'form-control' }) %>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
   <% end %> 
</div>
<div class="col-md-3"></div>
    </div>
  </div>
  <br><br><br>

