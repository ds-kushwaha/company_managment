<div class="container">
  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">      
<!--       <h2>Vertical (basic) form</h2> -->
        <%= form_for @city do |form| %>
    <div class="form-group">
      <label for="email">City:</label>
      <input type="text" class="form-control" id="city_city_name" placeholder="Enter city" name="city[city_name]">
    </div>
    <div class="form-group">
      <label for="pwd">State:</label>
      <%#= form.collection_select :country_id, Country.order(:country_name),:id,:country_name, include_blank: true, {:prompt => 'Select Country'} %>
      <%#= select("post", "person_id", Person.all.collect {|p| [ p.name, p.id ] }, {:prompt => 'Select Person'})%>

      <%= form.select(:state_id, State.all.map{|c| [c.state_name, c.id]}, {include_blank: "Select State"}, { :class => 'form-control' }) %>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
   <% end %> 
</div>
<div class="col-md-3"></div>
    </div>
  </div>
  

<br><br><br>