
 <div class="container">
  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">      
    <!--   <h2>Vertical (basic) form</h2> -->
        <%= form_for @company do |form| %>
    <div class="form-group">
      <label for="email">Company:</label>
      <input type="text" class="form-control" id="company_company_name" placeholder="Enter company" name="company[company_name]">
    </div>
    <div class="form-group">
      <label for="pwd">City:</label>
      <%#= form.collection_select :country_id, Country.order(:country_name),:id,:country_name, include_blank: true, {:prompt => 'Select Country'} %>
      <%#= select("post", "person_id", Person.all.collect {|p| [ p.name, p.id ] }, {:prompt => 'Select Person'})%>

      <%= form.select(:city_id, City.all.map{|c| [c.city_name, c.id]}, {include_blank: "Select City"}, { :class => 'form-control' }) %>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
    <%= link_to 'Back to Main Menu', welcome_index_path %>
   <% end %> 
</div>
<div class="col-md-3"></div>
    </div>
  </div>
  

<br><br><br> -->