<h1>Edit States</h1>
 <%= form_with model: @state, scope: :state, url: state_path(@state), local: true do |form| %>

 <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@state.errors.count, "error") %> prohibited
        this state from being saved:
      </h2>
      <ul>
        <% @state.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
    <p>
    <%= form.label :state %><br>
    <%= form.text_field :state_name %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<p>
  <strong>Title:</strong>
  <%= @state.state_name %>
</p>
 